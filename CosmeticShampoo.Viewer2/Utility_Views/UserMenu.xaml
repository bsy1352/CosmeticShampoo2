<UserControl x:Class="CosmeticShampoo.Viewer2.Utility_Views.UserMenu"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:iconPacks="http://metro.mahapps.com/winfx/xaml/iconpacks"
             xmlns:local="clr-namespace:CosmeticShampoo.Viewer2.Utility_Views"
             mc:Ignorable="d" BorderThickness="0" FontFamily="NanumSquare ExtraBold" Background="Transparent">
    <Grid>
        <iconPacks:PackIconMaterial Kind="{Binding Path=Icon}" VerticalAlignment="Center" Opacity="0.5" HorizontalAlignment="Left" Foreground="Black" Margin="10 16" Width="15"  Height="15" />
        <ListBoxItem x:Name="ListViewItemMenu" Content="{Binding Path=Header}" Padding="37 14" FontSize="15" Foreground="Black"/>
        <Expander x:Name="ExpanderMenu" Content="{Binding Path=Header}" IsExpanded="False" Width="210" HorizontalAlignment="Right" Background="{x:Null}" Foreground="Black">
            
        </Expander>
    </Grid>
</UserControl>
